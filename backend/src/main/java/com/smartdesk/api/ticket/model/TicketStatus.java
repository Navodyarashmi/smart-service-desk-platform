package com.smartdesk.api.ticket.model;

/**
 * Stages in the service desk ticket lifecycle.
 */
public enum TicketStatus {

    OPEN,
    ASSIGNED,
    IN_PROGRESS,
    RESOLVED,
    CLOSED,
    CANCELLED
}
